import { DescuentoPipePipe } from './descuento-pipe-pipe';

describe('DescuentoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DescuentoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
